{
  "name": "@onramp-sdk/tests-core",
  "version": "0.0.0",
  "private": true,
  "description": "Integration tests for @onramp-sdk/core",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@onramp-sdk/core": "workspace:*",
    "@onramp-sdk/shared": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^2.0.0",
    "typescript": "^5.5.0"
  }
}
