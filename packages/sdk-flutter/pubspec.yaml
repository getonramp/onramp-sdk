name: onramp_sdk
description: OnRamp SDK for Flutter — onboarding funnel visibility for mobile apps.
version: 0.2.0
homepage: https://getonramp.dev

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
